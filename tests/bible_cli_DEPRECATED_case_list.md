# bible_cli_DEPRECATED test case list

Python `bible_cli` has been renamed to `bible_cli_DEPRECATED` and is no longer
an active test target.

Current scan result: no existing test cases under `tests/` reference
`bible_cli`, `bible_cli_DEPRECATED`, or `python_cli`.

No test cases need to be removed together with `bible_cli_DEPRECATED/` at this
time.
